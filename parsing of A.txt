// Online C++ compiler to run C++ program online
#include <iostream>
#include <string.h>
#include <cstdlib>  //for atoi
#include <stdlib.h>  //for strtol

int main() {
    // Write C++ code here
    std::cout << "Try programiz.pro\n";
    char rx[]="A 3 8 [44 55] [99]";
   //std::cout <<rx;
    char* pch;
int i = 0;
pch = strtok(rx, " ,");
while (pch != NULL) {
    
    if (pch[0]=='['){
    std::cout << "open";
    }
    int l = strlen(pch);
    if (pch[l-1]==']'){
         std::cout << "clos";
    }
    
      //std::cout <<l <<" "<< atoi(pch);
      std::cout <<l <<" "<< strtol(pch,NULL,10);
      //both atoi and strtol will fail and return 0 if the first char is
      //non numeric and not a -+ sign
      
      
      //std::cout <<pch;
      
    std::cout <<"\n";
    
    
    ++i;
   //if (i>3) break;
pch = strtok(NULL, " ,");
}
     std::cout <<"exit "<<pch;
    

    return 0;
}


1 0
1 3
1 8
open3 0  << the string is [44 and it fails
clos3 55
openclos4 0
exit 










++++++++++++ https://www.programiz.com/cpp-programming/online-compiler/+++++++++
// Online C++ compiler to run C++ program online
#include <iostream>
#include <string.h>

int main() {
    // Write C++ code here
    std::cout << "Try programiz.pro\n";
    char rx[]="A 3 8 [44 55] []";
   //std::cout <<rx;
    char* pch;
int i = 0;
pch = strtok(rx, " ,");
while (pch != NULL) {
    
    if (pch[0]=='['){
    std::cout << "open";
    }
    int l = strlen(pch);
    if (pch[l-1]==']'){
         std::cout << "clos";
    }
    
      std::cout <<l <<" "<< pch;
      //std::cout <<pch;
      
    std::cout <<"\n";
    
    
    ++i;
pch = strtok(NULL, " ,");
}
    


1 A
1 3
1 8
open3 [44
clos3 55]
openclos2 []  <<we can find null arrays len=2 




// Online C++ compiler to run C++ program online   +++++++++++++++++++++++++++++++++++
#include <iostream>
#include <string.h>

int main() {
    // Write C++ code here
    std::cout << "Try programiz.pro\n";
    char rx[]="A 3 8 [44 55] []";
   //std::cout <<rx;
    char* pch;
int i = 0;
pch = strtok(rx, " ,");
while (pch != NULL) {
    
    if (pch[0]=='['){
    std::cout << "open";
    }
    int l = strlen(pch);
    if (pch[l-1]==']'){
         std::cout << "clos";
    }
    
      std::cout <<l <<" "<< pch;
      //std::cout <<pch;
      
    std::cout <<"\n";
    
    
    ++i;
    if (i>3) break;
pch = strtok(NULL, " ,");
}
     std::cout <<"exit "<<pch;
    

    return 0;
}




1 A
1 3
1 8
open3 [44
exit [44

I am thinking we have startBkt=true|false
endBkt=true|false

we expect to find startBkt, if we already found it, then if we encounter it again this is a parser error
if startBtk && endBkt ==true then we have an empty array

The key issue is how to return a pointer to the part of the input just after the parsed chunk.
Actually we don't need to.  Because we should have extracted "[numbers]" using our space-parser, and we call a subroutine
to just process this chunk.  also means we kinda expect [0]==[ and [strlen-1]==] else its not valid, but what if its
[342 [66 5 2]  this would slip thro

